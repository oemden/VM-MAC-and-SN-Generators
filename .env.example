# API Configuration
API_PORT=3000
# Optional: SQLite DB path (default: apps/api/data/vmgen.db)
# DATABASE_PATH=./data/vmgen.db

# Frontend Configuration (for production builds)
VITE_API_URL=http://localhost:3000
